// 原本是 package.json 但 github 一直提醒我有 "lodash" 漏洞 (挺聰明的) 所以改成 package_json

{
  "name": "blueprint",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "lodash": "4.17.11",
    "mustache": "^3.0.1",
    "raw-body": "^2.4.1"
  }
}
